name: Deploy Release (Requires Approval)

on:
  release:
    types: [published]
# on:
#   push:
#     tags:
#       - 'v*.*.*'   # triggers when a tag is pushed


permissions:
  contents: read         # minimal permission for checkout
  deployments: write     # needed if you were doing real deployments

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Simulate Deployment
        run: |
          echo "Deploying release ${{ github.event.release.tag_name }}"
          echo "Running fake deployment steps..."
          echo "Deployment complete!"
